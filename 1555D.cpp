#include <bits/stdc++.h>
using namespace std;
int main(){
	int n, m;
	cin >> n >> m;
	string s;
	cin >> s;
	while(m--){
		int l, r;
		cin >> l >> r;
		string subs = s.substr(l, r);
		int res = 0;	
		for(int i=0; i < n; i++){
			
		}
	}	
	return 0;
}
